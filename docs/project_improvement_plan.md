# PyCy_EMT_Lite 精简改进计划

[English](project_improvement_plan.en.md)

## 1. 目标与当前结论

保留小规模 EMT 教学项目，以 `CaseDefinition -> Circuit -> Simulator -> SimulationResult`
为主流程。优先保证时间、状态、事件与物理指标正确，再收缩重复内容。基础电路可以直接
调用 `Circuit/Simulator`，不必为每个使用场景增加一层包装。

基线核对于 **2026-09-05，HEAD `2aa55f9`**；以下明确区分已完成修改和现存缺陷。
当前工作区已实施部分任务，完成范围如下；未列为完成的缺陷仍然存在。
测试通过代表现有断言通过，不能证明高级模型物理正确。

| 任务 | 当前实施状态 |
|---|---|
| B-02 实例复用 | 已增加 Simulator/Circuit 单次运行和元件唯一所有权检查；注册编号前检查，已准备 Circuit 的浅拷贝也不能复用共享元件 |
| B-03 续算边界 | 已拒绝非零 `start_time`，删除仿真器中不可达的续算分支 |
| B-05 输入与错误 | 配置/事件时间、基本 RLC 参数及初值、独立源值拒绝 NaN/Inf；节点和元件名检查；标准事件目标提前验证，自定义事件保持兼容；求解错误保留原始原因、时间及求解器，stamp 错误保留元件和异常链。已补齐保留的开关、π/分段/Bergeron 线路、三相源/负荷和变压器参数检查，保留合法 0/None；分段数接受正整数及 NumPy 整数 |
| B-05 时间边界 | 已保留正时长的 0/stop，统一成对 8 ULP 舍入与事件出队；超出终点容差的事件不执行，近零正事件不被吞掉，内部网格/终点采用规范时间 |
| B-06 结果路径 | 已删除结果变换回调；示例 12 使用原始 `i:La/i:Lb/i:Lc` 字段，不再创建别名结果 |
| B-07 / B-10 指标 | 已按真实时间积分分段线性信号及电压/电流乘积；支持窗口截点，拒绝非法数据/跨事件插值；总有功包含零序，P/Q/S/PF 的适用范围已说明 |
| C 示例 07 | 已用故障前、中、后完整 50 Hz 周期的三相 RMS 替换有符号最低值；解析电阻分压检查通过，电路未修改 |
| C 控制示例标签 | 11、13–16 改用结果标签 `explicit_control` 描述手写状态更新；没有新增求解模式或修改算法 |
| D 最低工程门 | 已增强现有 wheel smoke：核对安装路径、版本和电阻求解；增加默认示例无 `outputs/` 检查，未新增 job/依赖 |
| B-01 / B-04 | B-01 未完成；B-04 已删除阻断式拓扑预检及无用模块，t=0 合闸电阻电路通过。初始化及动态事件左右极限仍未完成 |

本轮代码验证（2026-09-08）：**198 项测试通过**；16 个示例 headless 通过且未生成 `outputs/`；
新 wheel/sdist 不含本地缓存，源码外安装后原样执行 CI 的 wheel 检查代码通过。
这些结果不代表新的远端 CI 已运行。

历史基线为 65 项测试、16 个示例、43 个包内 Python 文件；用于说明审计起点，不是
当前待办清单。MIT、25 项顶层 API 和单一 CI job 已存在，不重新建设。
本轮已把时间边界和参数缺陷转为回归测试并修复；初始化与延时传播等临时探针仍不能代替持久回归。
独立审核发现的时间比较不一致已返工；审核 Agent 后续遇到额度限制，最终返工由主 Agent 自检。

## 2. 仍需解决的问题

“已复现”表示现有代码或运行结果已证明问题；“验证不足”表示还不能确认模型准确性。
P0 为基础数值错误，P1 为输入、时间边界或模型范围问题，P2 为说明维护。

| 优先级 / 任务 | 当前证据与影响 | 最小解决方向 |
|---|---|---|
| P0 / B-01 初始化，已复现 | `core/simulation.py::run` 首行按完整 dt 积分。1 V、1 kΩ、1 μF RC，dt=100 μs、stop=0、初始电压 0，仍输出 **0.047619 V** | 分开初始一致求解和时间推进；保持 vC/iL 初值，正确设置 iC/vL 历史，覆盖组合元件后再切换主循环 |
| P0 / B-04 动态事件，已复现 | 1 A 向 1 F 电容充电，dt=0.1 s，0.15 s 投入 1 Ω 故障，事件行 vC=**0.195122 V**。其中初始化已使 0.1 s 电压达到 0.15 V；从这个实际状态按旧拓扑继续 0.05 s 本应到 0.20 V，说明事件还反向改变了前一区间 | 先按旧拓扑走到事件左侧，再应用同刻事件，再固定储能状态求右侧代数量。两缺陷都修好后该事件行应为 vC=0.15 V、iC=0.85 A、iF=0.15 A |
| P1 / C 延时线边界，已复现 | Bergeron 接受 dt=1 ms、travel_time=0.5 ms；`lines.py::_delayed_sample` 在请求时间超出历史时直接取最后一帧，此时所需 0.5 ms 状态被 0 ms 状态代替，短延时并未准确求解 | 若保留则明确限制固定网格与对齐延时，提前拒绝超出合同的配置；不要靠最近历史值掩盖缺失状态 |
| P1 / B-08 物理验证不足 | RC/RL 只验证末值，RLC 只有有界/末值断言；线路缺交流幅相/传播断言，变压器缺负载下功率平衡；PWM 桥缺整桥基波及步长敏感性检查 | 在现有测试中加入固定物理窗口的解析、守恒和收敛断言；先修初始化/事件，再建立依赖正确时间语义的验收 |
| P1 / C 高级模型，方程缺口已确认 | 示例 15 用 `battery_power=load_power` 使 DC-link 功率差恒零；`VSCHVDCLinkAverage.step` 用功率差算线路电流，等功率输送损耗为零；MMC 臂 R/L 未参与输出方程 | 按第 4 节缩减候选，优先移除不在课程范围的实现及专属内容，不为保留名称补建耦合/器件框架 |
| P2 / D 文档收缩，部分完成 | 本轮已纠正程序指南的 checkpoint 续算说法与绘图旧导入；重复入口和超出保留范围的理论内容仍在 | 按保留模型合并现有文档；合并时保留有用信息，不增加另一份审计报告 |

已完成的单次运行、所有权、事件预验证、指标和零序功率检查继续保留。
`t=0` 合闸的纯电阻电路已经通过，不能将其当作动态事件正确的证据。

## 3. 后续执行顺序与验收

基础初始化仍是最高优先级，但不能阻塞可独立完成的输入/时间边界小修复。
每批沿用现有文件：先得到明确的失败断言，再修实现，由另一位 Agent 独立审核。
实现和常规执行用 Luna/Terra；Sol 只用于数值方案和独立审核，不扩展 Agent 层级。

| 顺序 | 工作范围和主要位置 | 结束条件 |
|---|---|---|
| 1，已完成 | `core/simulation.py::_time_points`、`events/base.py::pop_due` 与保留元件参数检查；测试放入 `test_time_grid.py`、`test_events.py` 及对应元件测试 | 极短正时长同时保留 0/stop；stop=0 只有一行；stop 外事件不被吸入；0.1+0.2 的舍入误差正常处理；合法零参数/None 语义不变，非法值在首次求解前失败 |
| 2，B-01 最小验证 | `test_basic_components.py`、`test_time_grid.py`、线路/三相/变压器测试；在临时实验中检查初始化方程 | 下述支持矩阵每类都有解析结果或明确拒绝理由；不要提交长期闲置的初始化器或增加模式开关 |
| 3，B-01 实现 | `components/basic.py` 和保留组合元件的现有状态；由 `Simulator.run` 调用同一内部一致求解过程 | t=0 无积分，零/非零初值及 stop=0 正确；支持范围内的线路/PWM 等保留示例不回退；冲突和不支持结构有明确错误 |
| 4，B-04 实现 | `Simulator.run` 的事件前后顺序、储能历史写回；沿用 EventQueue 稳定排序 | RC/RL 投入、清除、同刻、t=0、非对齐及 stop 事件通过；状态连续、代数量满足右侧 KCL/KVL，无重复时间或重复推进 |
| 5，B-08 与 C/D | 基础物理断言及第 4 节模型/文档收缩；明确淘汰的独立候选可提前删除 | 解析/能量/收敛验收通过；删实现时同步导出、专属测试、示例和双语文档；全套测试与保留示例通过 |

### 初始化：最小支持范围

初始化需要求两个不同的量：储能状态 **vC/iL** 和与网络一致的代数量 **iC/vL**。
不能只让第一行显示初值，而仍保留错误的历史项。t=0 先应用当刻事件，再求右侧一致初值。
后续正时长区间才调用积分 companion；`time_step=0` 不能直接传给含 1/dt 的旧 stamp。

| 必测结构 | 需要满足的约束 | 最小处理范围 |
|---|---|---|
| 普通 RC/RL/RLC | C 电压和 L 电流保持初值，结合电阻/源的 KCL/KVL 求代数量 | RC：1 V/1 Ω/1 F、v0=0.25 V 时 iC0=0.75 A；RL：1 V/1 Ω/1 H、i0=0.2 A 时 vL0=0.8 V；临时电容电流未知量只用于内部初始化 |
| 理想电压源并联电容 | vC(0)=Vs(0)；一致时 iC=C·dVs/dt | 冲突明确报错；只有此类约束确实需要时才索取源导数，常数导数为 0，已有正弦源可用解析导数 |
| 并联电容 | 初值电压一致，电流按电容比例分配 | 1 F、2 F 受 3 A 注入应得 dv/dt=1 V/s、支路 1 A/2 A；不是每种欠定结构都需要新增源回调 |
| 浮置电感星点 | 初始 KCL 和其导数同时成立 | 等值电感、相端 400/0/0 V、零电流时星点为 400/3 V；不靠接地泄漏或更改中性点消除欠定 |
| π 线路、三相 RLC、线性变压器 | 内部每个储能支路也必须满足相同初值/代数关系 | 局部读取现有节点、参数及状态并写回，与独立拆分等效电路比较初值/首步；不建设全模型注册表或反射接口 |

任意 `value(t)` callable 不能保证提供精确导数。普通满秩网络仍可保留现有 callable；
仅在必要的退化约束中，考虑给 VoltageSource/CurrentSource 增加可选导数回调，并检查有限性。该接口目前
**尚未实现**，需要先由上述算例证明必要性和充分性，不引入波形类体系或数值差分兜底。

冻结状态后的矩阵欠定不等于初值冲突：可用明确的导数约束补足前者；没有足够输入时
报不支持，初值违反理想约束时报告冲突。保留现有线性求解器，不用任意正则化、epsilon
步、无物理约束的最小二乘或通用 DAE 框架。独立冗余的理想源若无法唯一分配电流，
也不能强行生成支路结果。

目标限于直接可解或增加一次导数约束可解的线性 RLC；仍欠定或需要更高阶约束时明确
拒绝。导数约束与初值一致性检查分开，不把矩阵求解成功当作理想约束已满足。

先在临时实验与现有测试中验证普通 RLC，再补齐保留组合件和必要约束，最后切换主循环。
不能因线路/PWM 较复杂而永远搁置基础修复，也不能悄悄给不支持的结构回退到旧的 t=0
积分。饱和/电机/延时历史等未纳入本次线性初始化的模型，应在切换前按课程范围决定
删除、明确拒绝或另有直接验证的专用处理；不承诺这一方案已经覆盖它们。

### 动态事件：固定执行顺序

1. 用事件前网络把 `(t_prev, t_event)` 积分到左极限，储能状态只推进这一次。
2. 按声明顺序应用同刻事件；保留逐条日志，以全部事件后的最终网络只求一次右极限。
3. 固定刚得到的 vC/iL，复用一致求解得到右侧 iC/vL；不再用正 dt 做第二次积分。
4. 写回右侧代数量作为下一段梯形历史：电容更新 `previous_current/last_current`，
   电感更新 `previous_voltage/last_voltage`，组合内部状态同样处理；储能值不再改变。
5. 每个时间点只输出一行，事件时输出右侧值，下一段从该状态继续；stop 同刻事件也要执行并输出右侧值。

无冲激的有限电阻切换应保持状态连续；理想连接造成初值冲突、强迫电容电压跳变或
电感电流中断时，当前范围明确报错，不模拟冲激。正常连续步不得重新初始化。

以上首先处理显式事件。callable 源跳变及 PWM 门极边沿没有自动左右极限信息，不能
声称插入事件时间就已解决；PWM 仍按实际网格采样，单独验证步长敏感性。若保留示例的
边沿处理确需扩展，应先给具体失败算例，避免通用过零检测器。现有 backward_euler
可用于切换参考；自动切换积分方法尚未实现，也不是本轮默认解决方案。

### 时间边界与延时线

结果时间必须有限、严格递增，首点为 0、末点为 stop；正时长不能被容差合并成单点。
约定距规范网格/终点不超过成对时间中较大 ULP 的 8 倍时视为同刻，ULP 是相邻浮点数的间隔；
float 无法区分人为 nextafter 和运算舍入，因此“stop 外事件”指超过此阈值。靠近 0 时
仅精确 0 视为 0，不吞掉正时刻；阈值不能由秒级常数给所有小时间设置绝对下限。
先在生成网格时确定规范时间，再让事件出队采用同一规则；不分别放宽终点筛选、对齐
和 pop_due。覆盖阈值内/外、stop 前/同刻/后、靠近 0、连续近邻事件及三种策略。

Bergeron 是可选模型：若保留，第一步只承诺 `travel_time>=dt` 且传播时延、停止时间和
实际事件时间均与固定 dt 对齐；在首次求解前拒绝短末步或插点，不改写历史插值框架。
到达前无响应、匹配负载到达幅值、开路/短路反射才是验收；“有字段且有限”不能证明
传播正确。延时波前跨历史插值区间的正确性另行验证，不混同于普通 RLC 的可变步能力。

### 指标与物理测试

对连续窗口，先规定信号采用分段线性重建。每段时长 h、端值 a/b：
均值积分为 `h*(a+b)/2`，平方积分为 `h*(a*a+a*b+b*b)/3`；再除以窗口总时长，
RMS 最后开方。这使沿同一线段增加冗余点时指标保持不变。窗口截点也沿同一线段求值。
平均功率先重建电压和电流，再积分二者乘积；不能先将端点相乘再线性积分，否则会破坏
电阻电路的 `P=Vrms²/R`。P/Q 共用同一乘积积分规则，不另建信号处理框架。

不能把 `trapezoid(x*x,time)` 的近似误差当作必须严格不变的条件；如果选择该近似方案，
验收应改为随步长收敛。时间列必须有限、严格递增；空窗、单点、零时长或越界窗口明确
报错。故障统计使用不跨拓扑跳变的 pre/fault/post 窗口，排除边界邻近区间；跨事件积分
不在本轮实现。瞬时总有功支持逐相求和；现有 αβ q 和 `sqrt(P²+Q²)` 的 S/PF 只作为
已说明条件下的量，不用于泛化的不平衡/畸变功率质量结论。

B-08 在现有测试中覆盖 R、RC、RL、RLC 的解析值、KCL/KVL、无源能量和步长减半误差；
误差比较固定物理时间窗与共同时间点，排除事件点后再判断光滑区间收敛阶，不要求所有
事件算法都有二阶收敛。三相、线路和变压器验证见下表。B-09 沿用现有绘图/导出测试，
不为全部示例重复“显示/保存/不保存”组合。

## 4. 模型、示例和文档收缩

下表为后续实施决定，不表示本次已删除文件。每次删除同时检查源码导入、子包导出、
测试、示例、README 和专属文档；用 Git 查看历史，不另建归档目录。

| 模型 / 示例 | 最小保留范围及验收 |
|---|---|
| R/L/C/独立源；01–04 | 保留；01 是 Quick Start，02–04 可作为一个学习单元。小脚本独立更清楚时无需合成多模式大脚本 |
| 三相/故障；05–07 | 保留平衡、三相短路及一个不对称故障；验证固定 pre/fault/post RMS、相序和逐相功率。07 的 RMS 摘要及解析分压检查已完成 |
| π 线路；08 | 保留一个集中参数线路课，补交流稳态幅相及电流/能量检查 |
| 单相变压器；09 | 保留，补负载变比、漏抗及功率平衡；多段线路/三相变压器只有明确课程用途时保留 |
| Bergeron | 当前无对应示例，不作为必做课程；若保留，只先支持 `travel_time>=dt` 且延时、停止与事件均对齐固定 dt，验证无预响应、到达及反射，不升级历史插值框架 |
| IdealSwitch/PWM；12 | 保留离散采样开关示例，验证相电流、基波和步长敏感性；已去掉仅为别名使用的 `result_transform` |
| 变换、PI、PLL；11 | 保留最终课程实际使用且有直接测试的部分；手写状态更新的方法标签已修正，不强制将纯控制演示塞进 MNA |
| 平均 GFL/GFM；13–15 | 最多选一个综合案例，优先评估 13；14 合并或删除，15 只有能量平衡与独立参考通过后才可能替代 13。不能小幅接入主流程就本版不保留综合案例，不为它新建耦合框架 |
| Park 同步机；10 | dq 端口与参数作用未充分验证；降级为清楚命名的代理，或连同专属内容删除，不实现完整 Park 来挽救名称 |
| Diode/反并联 IGBT、详细 PV、VSC-HVDC/MMC；16 | 当前从最终范围排除，按调用关系移除实现/示例/专属文档；不建设通用 Newton、活动集或完整器件库 |
| 拓扑启发检查 | 已去掉阻断仿真的用途及随之无用的代码/导入；奇异性依赖已有求解器，不另建拓扑边协议 |

目标是约 6–8 个学习单元，**不是强制文件配额**。保留两个短 RC/RL 脚本可能比增加分支
和配置更精简。没有独立参考的高级模型退出核心范围，不拖延基础核心改进。

文档最终收敛到 README 入口、数值约定、模型与验证三个主题。中英文同步合并，保留
现有有用信息和语言入口；不把已有完整译文缩成摘要，不扩展翻译体系。

| 当前内容 | 合并去向 |
|---|---|
| `user_guide`、`new_simulation_workflow` | README 的必要操作，去掉重复长代码 |
| `simulation_program_guide`、`theory/mna`、`basic_components`、`stamp_principles` | 数值约定：初值、时间、事件、单位、方向及少量基础 stamp |
| 其余理论文件 | 模型与验证：只保留最终模型的方程、限制、现有证据和未验证项 |
| `visualization_reporting` | 必要绘图用法进入 README/docstring，删除旧 API 内容 |
| 本计划及英文版 | 改进期间保留；任务完成后移除，不建设独立审计报告 |

目标文件 `numerical_conventions.md`、`models_and_validation.md` 只在真实合并时创建，
同批移除被替代内容、更新链接。中英文共同修改的语义同步；不设全文 parity 门。
计划和本轮修正段落同步英文。链接检查可先用一次性命令；只有重复发生回归才加入小测试，
不建立文档解析器或网站。

## 5. 最低工程门与停止条件

沿用一个 Ubuntu/Python 3.14 CI job：锁定安装、pytest、Quick Start、保留示例 headless、
构建、源码外 wheel 导入/版本检查。已有 wheel 命令已包含 R 电路解析断言，
不为它创建新脚本。核对实际 import 路径来自已安装 wheel，不能由源码路径掩盖漏打包。

`git status --porcelain` 只能发现未忽略的变化，不能证明被忽略的 `outputs/` 没有生成。
默认示例关闭保存；已有 CI 已加入 `outputs/` 不存在断言。
本机缓存无需为通过检查而删除。远端 CI 必须注明 commit；本地等价验证不能替代远端状态。

新增文件、类或依赖前只问三件事：哪个已确认的问题需要它，为什么现有内容容纳不了，
怎样最小验证。**不要求新增缺陷测试必须删除一个旧文件**；也不为凑数量合并本来清楚的模块。
保留现有 NumPy/SciPy/Matplotlib/pytest，不为了零依赖重写求解、绘图或测试工具。

一个数值任务完成需同时满足：原失败算例通过；固定时间窗/单位/方向/容差的物理断言；
现有相关回归和保留示例通过；只更新最近的必要说明；没有新闲置内容。文档任务完成需
链接有效、去向清楚、差异检查通过，不重复运行无关数值检查。不存在未验证的“已完成”。

当前不建设：第二套 API、YAML/CaseSpec、课程 CLI、通用非线性/控制耦合框架、reset/
checkpoint、稀疏/流式/LU 缓存优化、模型卡/manifest/报告生成器、文档站、复杂 CI
矩阵、覆盖率门或定时维护任务。性能优化只由默认课程的 profiler 证据触发；额外平台
检查只在声明支持范围变化或复现平台问题时添加；发布设施只在实际决定发布时考虑。

## 6. 必要参考

- [NumPy trapezoid](https://numpy.org/doc/stable/reference/generated/numpy.trapezoid.html)：可传入真实横坐标，但不会自动排序；不等于对任意插点严格不变的平方积分。
- [PSCAD Interpolation and Switching](https://www.pscad.com/webhelp-v5-ol/EMTDC/Advanced_Features/interpolation_and_switching.htm)：事件时刻与步长对切换误差的影响；本项目只据此定义需要验证的边界，不照搬完整插值机制。

外部资料用于核对定义，项目可信性仍由自己的解析、守恒、收敛和独立参考证明。
